<div class="feed-event-date">
  <%=l feed_event.created_at -%>
</div>
<div class="feed-event-content">
  <%= feed_event.trigger.username -%>
  <%- unless feed_event.operator.blank? -%>
  (Nutzer <%= link_to feed_event.operator.full_name, user_path(feed_event.operator) -%>)
  <%- end -%>
  hat <%= number_to_currency(feed_event.trigger.real_amount, :precision => 0) -%> für
  <%- unless feed_event.trigger.activity.blank? -%>                                            
  die Aktion <%= link_to feed_event.trigger.title, activity_path(feed_event.trigger.activity) %> der Organisation 
  <%- else-%>
  die Organisation
  <%- end -%>
  <%= link_to feed_event.trigger.organisation.name, organisation_path(feed_event.trigger.organisation) %> gespendet.
</div>  

