<%=h I18n.t(:"activity.created.user_email.message", :user_name => @user.full_name,
                                                    :url => @url,
                                                    :activity_name => @activity.title) %>