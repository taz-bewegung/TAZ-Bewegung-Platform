<div id="categories" class="edit-container">
  
  <%= render :partial => "/shared/profile_edit_header", :locals => { :header => "Kategorien", 
                                                                 :object => @activity,
                                                                 :path => edit_part_my_helpedia_activity_path(@activity, :part => "categories"),
                                                                 :prefix => "categories",
                                                                 :button => image_tag("buttons/btn-edit.png")
                                                                } -%>  

  <div class="clearfloats edit-profile">
    <div class="two-column">
      <div class="column-100">
        <dl>    
          <dt><%= Activity.human_attribute_name("social_categories") -%>:</dt>
          <dd><%= @activity.social_categories.map(&:title).join(", ") || "-" -%></dd>
      </div>
    </div>
  </div>
</div>
